class Over:
    def __init__(self):
        pass

    def sum(self, *x):
        add=0
        for i in x:
            add+=i
        return add
    
    def sum(self, *x):
        add=0
        for i in x:
            add+=i
        return add
    
    def sum(self, *x):
        add=0
        for i in x:
            add+=i
        return add
    

S=Over()
print(S.sum(1, 2))
print(S.sum(1, 2, 3))
print(S.sum(1, 2, 3, 4))